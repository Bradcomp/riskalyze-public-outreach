function Security(symbol, name, riskNumber, bestCase, worstCase) {
    this.symbol = symbol;
    this.name = name;
    this.riskNumber = riskNumber;
    this.bestCase = bestCase;
    this.worstCase = worstCase;
}